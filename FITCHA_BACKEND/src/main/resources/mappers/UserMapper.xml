<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.fitcha.model.dao.UserDao">

	<!-- 유저 회원가입 (등록) -->
	<insert id="insertUser" parameterType="User">
		INSERT INTO user_board (user_id, password, email, name, nick_name, age, gender)
		VALUES
		(#{userId}, #{password}, #{email}, #{name}, #{nickName}, age, gender)
	</insert>


	<!-- 유저 탈퇴 (삭제) -->
	<delete id="deleteUser" parameterType="int">
		DELETE 
		FROM user_board
		WHERE user_board_id = #{userBoardId}
	</delete>
	
	<!-- 유저 로그인 -->
	<!-- 로그인 요청한 id와 DB에 등록된 동일한 id가 있으면 null이 아닌 유저가 반환 된다. -->
	<select id="selectRegistedUser" parameterType="User" resultType="User">
		SELECT *
		FROM user_board
		WHERE user_id = #{userId}
	</select>

</mapper>